import config from '../../config/index';
let sysPre = config.prefixs.sysPre;

let html = () => {
    return (
        '<div>\
            <div>domains</div>\
        </div>'
    );
};

export default html();
